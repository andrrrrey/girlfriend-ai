## Git Conventions
- Git branches should be prefixed with `feature/` or `fix/` followed by task description
- Branch naming format: `feature/TICKET-123-description` or `fix/bug-description`
- Never use "claude" prefix in branch names
